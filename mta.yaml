_schema-version: "3.1"
ID: service_logs
description: A simple CAP project.
version: 1.0.0
modules:
- name: service_logs-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: service_logs-db
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
  build-parameters:
    builder: npm
- name: service_logs-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: service_logs-db
  parameters:
    buildpack: nodejs_buildpack
- name: service_logs-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: service_logs-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - cpappservicelogsdetails.zip
      name: cpappservicelogsdetails
      target-path: resources/
- name: cpappservicelogsdetails
  type: html5
  path: app/service_logs_details
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: service_logs-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: service_logs-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: service_logs-html5-srv
    service-plan: app-host
- name: service_logs-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: service_logs-destination-service
    service-plan: lite
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npx cds build --production
